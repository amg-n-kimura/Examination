学籍番号[A]と名前[B]を入力,A,B
オブジェクト指向言語とは、データ構造とアルゴリズム（プログラム）を１つにまとめてカプセル化したものである。○か、☓か。[A],A
オブジェクト指向言語では、プログラム言語として記述されたものを[A]と呼び、実行時のメモリイメージを[B]と呼ぶ。また、それらを含めた抽象的な表現として[C]と表現することもある。,A,B,C
クラスは他のクラスを継承して、その機能やデータ構造等を引き継ぐ事が出来る。この時、継承元となるクラスを[A]クラス、継承先となるクラスを[B]クラスと呼ぶ,A,B
[A]とはJava Virtual Machine（仮想マシン）の略称で、様々なOS上でJAVAのプログラムを実行させるためのアプリケーションである。,A
Javaでは、C言語で言う所の「関数」は[A]と呼ぶ。また、クラス内の変数は[B]と呼ぶ。,A,B
Javaでは、すべてのクラスは必ず1つのパッケージ（名前空間）に属する。パッケージはOS上のフォルダ構成を.（ピリオド）で区切って表現する。その際は全て[A]で表記する。,A
Javaでは、ファイル名とクラス名をアッパー[A]ケースで記述し、それ以外は基本的にロワー[B]ケースで記述する。（AとBは同じ単語が入る）,A,B
Javaのソースコードの記述において、クラスの継承は[A]という予約語を使い、その他のインターフェースの実装は[B]という予約語で記述する。,A,B
Javaでは、継承可能なクラスはいくつまでか？[A]つ。,A
Javaでは、継承した際に{第6問のA}を同一名同一引数で記述することを[A]と呼ぶ。,A
Javaでは、クラス内で同一名の異なる引数を持つ{第6問のA}を記述することを、[A]と呼ぶ。,A
Javaのクラスや{第6問のA}、{第6問のB}は、それぞれスコープ（参照可能範囲）を指定出来る。全てのクラス（オブジェクト）から参照可能なスコープを[A]、継承先及び同一パッケージのクラスからのみ参照可能なスコープは[B]、自クラスのみ参照可能なスコープを[C]と記述する。,A,B,C
クラスがインスタンス化される際に呼び出される{第6問のA}を、[A]と呼ぶ。,A
Javaではプリミティブ型に対してそのままクラスとして参照出来るように定義されたラッパークラスが用意されている。byteに対してはByte、charに対しては[A]、intに対しては[B]、longに対してはLong、floatに対していはFloat、doubleに対してはDoubleとなっている。,A,B
Javaでは、配列は実行時にインスタンスの割当を行う。○か、☓か。[A],A
JavaにはList、Set、Map等、[A]クラスと呼ばれる様々なデータ管理クラスがある。,A
Javaでは、スレッドを生成・起動・制御するためのクラスとして、[A]クラスが用意されている。このクラスを利用するには。継承するか、[B]インターフェースを実装する必要がある。,A,B
[A]クラスは抽象化クラスとも呼ばれ、クラス全体もしくは一部を抽象化して、必ず継承させることで仕様を守らせる事ができる。また、[B]クラスは抽象化された{第5問のA}のみで構成された抽象化クラスである。,A,B
Gitはソースファイルなどの[A]管理をグループで行う事ができるサービスである。,A
Gitは「ギット」と読む。○か、×か。,A
Gitではローカル上でファイルを更新することを「[A]」と呼び、コミット結果をサーバーに同期させることを「[B]」と呼ぶ。,A,B
前問とは逆に、サーバーの状態をローカルに同期させることを「[A]」と呼ぶ。,A
Gitではリポジトリ管理する際に必ず「ブランチ」という名称を割り当てる。このブランチ名をもとに管理するため、同一リポジトリ上で複数のユーザーが同時に別々の状態を持つことが出来る。また、ブランチ間で同期を取ってファイルの差分を更新することも出来る。この事を「[A]」と呼ぶ。,A
{第24問のA}を実行した際に、異なるブランチ間で同一箇所を同時に編集していた結果起きる競合状態のことを[A]と呼ぶ。,A